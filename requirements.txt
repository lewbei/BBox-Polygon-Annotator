# BBox & Polygon Annotator - Core Dependencies
# Image processing and computer vision
opencv-python>=4.5.0
Pillow>=8.0.0
numpy>=1.20.0

# Data serialization and configuration
PyYAML>=5.4.0

# AI/ML - YOLO auto-annotation (optional but recommended)
ultralytics>=8.0.0

# Additional ML dependencies for YOLO (will be installed with ultralytics)
torch>=1.8.0
torchvision>=0.9.0

# Note: tkinter comes with Python standard library
# Note: All other dependencies (json, logging, os, etc.) are part of Python standard library
